mixin card-description(img="icon_check.svg", width="30")
    .row.align-left.align-top
        .card__img-wrapper
            img(src="./img/icons/"+img alt=img width= width)
        p.poduct-card__text
            block